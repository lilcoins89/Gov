import { Check, ChevronDown, Filter, MoreHorizontal, Search, UserRound } from 'lucide-react';
import { useMemo, useState } from 'react';
import { Link } from 'wouter';
import { DashboardSidebar } from '@/components/casework/DashboardSidebar';
import { StatusBadge } from '@/components/casework/StatusBadge';
import { formatCurrency, formatDate, loadCases, reviewers, saveCases, type CaseStatus } from '@/data/casework';

export default function Admin() {
  const [cases, setCases] = useState(loadCases());
  const [status, setStatus] = useState<'All' | CaseStatus>('All');
  const [query, setQuery] = useState('');
  const filtered = useMemo(() => cases.filter((item) => (status === 'All' || item.status === status) && `${item.id} ${item.title} ${item.institution}`.toLowerCase().includes(query.toLowerCase())), [cases, query, status]);
  const updateStatus = (id: string, next: CaseStatus) => { const updated = cases.map((item) => item.id === id ? { ...item, status: next, lastUpdate: new Date().toISOString().slice(0, 10) } : item); setCases(updated); saveCases(updated); };
  const assign = (id: string, reviewer: string) => { const updated = cases.map((item) => item.id === id ? { ...item, assignedReviewer: reviewer } : item); setCases(updated); saveCases(updated); };
  return <div className="min-h-dvh bg-background"><DashboardSidebar staff /><main className="md:pl-[17.5rem]"><div className="mx-auto max-w-7xl px-5 pb-16 pt-20 md:px-8 md:pt-10"><header className="flex flex-col gap-5 border-b border-border pb-8 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-sm font-semibold text-muted-foreground">Operations · Monday, June 10</p><h1 className="mt-2 font-display text-4xl font-extrabold tracking-[-0.05em] text-navy-900">Review overview</h1><p className="mt-2 text-muted-foreground">Keep the queue clear, focused, and properly assigned.</p></div><div className="rounded-xl border border-teal-200 bg-teal-50 px-4 py-3 text-sm font-bold text-teal-900"><span className="mr-2 inline-block h-2 w-2 rounded-full bg-teal-600" />All systems operational</div></header><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Open queue</p><p className="mt-3 font-display text-3xl font-extrabold">{cases.filter((item) => !['Resolved', 'Closed'].includes(item.status)).length}</p><p className="mt-1 text-xs text-muted-foreground">Cases needing active review</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Information requests</p><p className="mt-3 font-display text-3xl font-extrabold">{cases.filter((item) => item.status === 'Information Required').length}</p><p className="mt-1 text-xs text-muted-foreground">Waiting on client detail</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Reviewers online</p><p className="mt-3 font-display text-3xl font-extrabold">{reviewers.length}</p><p className="mt-1 text-xs text-muted-foreground">Across three specialties</p></div></div><section className="mt-10 rounded-2xl border border-border bg-card soft-shadow"><div className="flex flex-col gap-4 border-b border-border p-5 lg:flex-row lg:items-center lg:justify-between"><div><h2 className="font-display text-xl font-extrabold">Case queue</h2><p className="mt-1 text-sm text-muted-foreground">{filtered.length} records match the current view</p></div><div className="flex flex-col gap-2 sm:flex-row"><label className="relative"><Search size={16} className="pointer-events-none absolute left-3 top-3.5 text-muted-foreground" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search cases" className="w-full rounded-xl border border-input bg-background py-3 pl-9 pr-3 text-sm outline-none focus:border-teal-600 sm:w-52" data-testid="input-search-cases" /></label><label className="relative"><Filter size={15} className="pointer-events-none absolute left-3 top-3.5 text-muted-foreground" /><select value={status} onChange={(event) => setStatus(event.target.value as 'All' | CaseStatus)} className="w-full appearance-none rounded-xl border border-input bg-background py-3 pl-9 pr-9 text-sm outline-none focus:border-teal-600 sm:w-52" data-testid="select-filter-status"><option>All</option><option>Submitted</option><option>Under Review</option><option>Information Required</option><option>In Progress</option><option>Resolved</option><option>Closed</option></select><ChevronDown size={15} className="pointer-events-none absolute right-3 top-3.5 text-muted-foreground" /></label></div></div><div className="hidden overflow-x-auto md:block"><table className="w-full text-left"><thead className="border-b border-border bg-muted/40 text-xs font-bold uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-3">Case</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Reviewer</th><th className="px-5 py-3">Updated</th><th className="px-5 py-3" /></tr></thead><tbody className="divide-y divide-border">{filtered.map((item) => <tr key={item.id} className="transition hover:bg-muted/30"><td className="px-5 py-4"><Link href={`/dashboard/cases/${item.id}`} className="font-bold text-card-foreground hover:text-teal-800">{item.title}</Link><p className="mt-1 text-xs text-muted-foreground">{item.id} · {item.institution}</p></td><td className="px-5 py-4"><select value={item.status} onChange={(event) => updateStatus(item.id, event.target.value as CaseStatus)} className="mb-2 block rounded-lg border border-border bg-background px-2 py-1 text-xs font-bold outline-none" data-testid={`select-status-${item.id}`}><option>Submitted</option><option>Under Review</option><option>Information Required</option><option>In Progress</option><option>Resolved</option><option>Closed</option></select><StatusBadge status={item.status} /></td><td className="px-5 py-4 text-sm font-bold">{formatCurrency(item.amountInvolved)}</td><td className="px-5 py-4"><label className="flex items-center gap-2 text-sm"><UserRound size={15} className="text-muted-foreground" /><select value={item.assignedReviewer} onChange={(event) => assign(item.id, event.target.value)} className="max-w-[130px] bg-transparent text-sm font-semibold outline-none" data-testid={`select-reviewer-${item.id}`}><option>Pending assignment</option>{reviewers.map((reviewer) => <option key={reviewer.id}>{reviewer.name}</option>)}</select></label></td><td className="px-5 py-4 text-sm text-muted-foreground">{formatDate(item.lastUpdate)}</td><td className="px-5 py-4"><button type="button" onClick={() => window.alert(`Actions for ${item.id}`)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted hover:text-foreground" aria-label={`More actions for ${item.id}`} data-testid={`button-case-actions-${item.id}`}><MoreHorizontal size={17} /></button></td></tr>)}</tbody></table></div><div className="space-y-3 p-4 md:hidden">{filtered.map((item) => <div key={item.id} className="rounded-xl border border-border p-4"><div className="flex items-start justify-between gap-2"><Link href={`/dashboard/cases/${item.id}`} className="font-bold">{item.title}</Link><StatusBadge status={item.status} /></div><p className="mt-1 text-xs text-muted-foreground">{item.id} · {formatCurrency(item.amountInvolved)}</p><div className="mt-4 grid gap-2"><select value={item.status} onChange={(event) => updateStatus(item.id, event.target.value as CaseStatus)} className="rounded-lg border border-border bg-background px-3 py-2 text-xs font-bold" data-testid={`mobile-select-status-${item.id}`}><option>Submitted</option><option>Under Review</option><option>Information Required</option><option>In Progress</option><option>Resolved</option><option>Closed</option></select><select value={item.assignedReviewer} onChange={(event) => assign(item.id, event.target.value)} className="rounded-lg border border-border bg-background px-3 py-2 text-xs font-bold" data-testid={`mobile-select-reviewer-${item.id}`}><option>Pending assignment</option>{reviewers.map((reviewer) => <option key={reviewer.id}>{reviewer.name}</option>)}</select></div></div>)}</div>{filtered.length === 0 && <div className="p-12 text-center"><Check size={24} className="mx-auto text-teal-700" /><p className="mt-3 font-bold">No cases match this view.</p><p className="mt-1 text-sm text-muted-foreground">Try another status or search term.</p></div>}</section></div></main></div>;
}