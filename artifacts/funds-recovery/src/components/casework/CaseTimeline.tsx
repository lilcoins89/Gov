import { Check, Circle, Clock3 } from 'lucide-react';
import { formatDate, type StatusEvent } from '@/data/casework';

export function CaseTimeline({ events }: { events: StatusEvent[] }) {
  return <div className="space-y-0" data-testid="case-timeline">{events.map((event, index) => <div key={event.id} className="relative flex gap-4 pb-7 last:pb-0"><div className="relative z-10 mt-0.5 grid h-8 w-8 shrink-0 place-items-center rounded-full border-2 bg-card">{event.completed ? <span className="grid h-6 w-6 place-items-center rounded-full bg-teal-700 text-white"><Check size={14} strokeWidth={3} /></span> : event.current ? <span className="grid h-6 w-6 place-items-center rounded-full bg-teal-100 text-teal-800"><Clock3 size={14} /> </span> : <Circle size={15} className="text-muted-foreground/60" />}</div>{index < events.length - 1 && <span className={`absolute left-[15px] top-8 h-full w-px ${event.completed ? 'bg-teal-200' : 'bg-border'}`} />}<div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className={`font-bold ${event.current ? 'text-teal-800' : 'text-card-foreground'}`}>{event.label}</h3>{event.current && <span className="rounded-full bg-teal-50 px-2 py-0.5 text-[10px] font-bold uppercase tracking-wider text-teal-800">Current</span>}</div><p className="mt-1 text-sm leading-6 text-muted-foreground">{event.description}</p><p className="mt-2 text-xs font-semibold text-muted-foreground">{formatDate(event.date)}</p></div></div>)}</div>;
}