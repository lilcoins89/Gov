import { ArrowUpRight, BriefcaseBusiness, CheckCircle2, Clock3, MessageSquareText } from 'lucide-react';

export function StatsCards({ activeCount, underReviewCount, attentionCount, closedCount }: { activeCount: number; underReviewCount: number; attentionCount: number; closedCount: number }) {
  const stats = [{ label: 'Active Cases', value: activeCount, hint: 'Open in your workspace', icon: BriefcaseBusiness }, { label: 'Cases Under Review', value: underReviewCount, hint: 'Currently being reviewed', icon: Clock3 }, { label: 'Information Required', value: attentionCount, hint: 'Waiting on your response', icon: MessageSquareText }, { label: 'Closed Cases', value: closedCount, hint: 'Completed case records', icon: CheckCircle2 }];
  return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(({ label, value, hint, icon: Icon }, index) => <div key={label} className={`fade-up fade-up-delay-${Math.min(index + 1, 3)} rounded-2xl border border-border bg-card p-5 soft-shadow`} data-testid={`stat-${label.toLowerCase().replaceAll(' ', '-')}`}><div className="flex items-start justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-teal-50 text-teal-800"><Icon size={19} /></span><ArrowUpRight size={16} className="text-muted-foreground/60" /></div><p className="mt-5 font-display text-3xl font-extrabold tracking-[-0.04em] text-card-foreground">{value}</p><p className="mt-1 text-sm font-bold text-card-foreground">{label}</p><p className="mt-1 text-xs text-muted-foreground">{hint}</p></div>)}</div>;
}